# Vectimus Sentinel

Read `AGENTS.md` at the repo root for pipeline overview, then each agent spec in `agents/*/AGENTS.md`.

See `HANDOVER.md` for build sequence, technical decisions, dependencies and configuration.
